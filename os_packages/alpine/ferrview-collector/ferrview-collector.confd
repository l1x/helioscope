# Configuration for /etc/init.d/ferrview-collector

# User (and group) to run ferrview-collector as.
#command_user="ferrview:ferrview"

# Additional arguments for ferrview-collector daemon.
# By default, ferrview-collector looks for its config at /etc/ferrview/ferrview-collector.toml
#command_args="--config /etc/ferrview/ferrview-collector.toml"

# Comment out to disable process supervisor.
supervisor=supervise-daemon
