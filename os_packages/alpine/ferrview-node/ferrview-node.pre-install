#!/bin/sh

# Create ferrview user and group if they don't exist
addgroup -S ferrview 2>/dev/null
adduser -S -D -H -h /var/lib/ferrview -s /sbin/nologin -G ferrview -g "Ferrview Service User" ferrview 2>/dev/null

exit 0
